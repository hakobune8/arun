apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "arun.fullname" . }}
  labels:
    {{- include "arun.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "arun.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "arun.selectorLabels" . | nindent 8 }}
    spec:
      {{- $podSecurityContext := default dict (get .Values "podSecurityContext") }}
      {{- $securityContext := default dict (get .Values "securityContext") }}
      {{- $capabilities := default dict (get $securityContext "capabilities") }}
      {{- $env := default dict (get .Values "env") }}
      {{- $secrets := default dict (get .Values "secrets") }}
      {{- $secretName := default (include "arun.fullname" .) (get $secrets "existingSecret") }}
      securityContext:
        fsGroup: {{ default 101 (get $podSecurityContext "fsGroup") }}
        runAsGroup: {{ default 101 (get $podSecurityContext "runAsGroup") }}
        runAsNonRoot: {{ default true (get $podSecurityContext "runAsNonRoot") }}
        runAsUser: {{ default 100 (get $podSecurityContext "runAsUser") }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: {{ default false (get $securityContext "allowPrivilegeEscalation") }}
            capabilities:
              drop:
                {{- default (list "ALL") (get $capabilities "drop") | toYaml | nindent 16 }}
            readOnlyRootFilesystem: {{ default false (get $securityContext "readOnlyRootFilesystem") }}
          args:
            - serve
            - --port
            - "{{ .Values.env.ARUN_PORT }}"
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          {{- if .Values.probe.enabled }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: {{ .Values.probe.initialDelaySeconds }}
            periodSeconds: {{ .Values.probe.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: {{ .Values.probe.initialDelaySeconds }}
            periodSeconds: {{ .Values.probe.periodSeconds }}
          {{- end }}
          env:
            - name: HOME
              value: "/home/arun"
            - name: LITELLM_BASE_URL
              value: "{{ .Values.env.LITELLM_BASE_URL }}"
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: LITELLM_API_KEY
                  optional: true
            - name: ARUN_MODEL_CODER
              value: "{{ .Values.env.ARUN_MODEL_CODER }}"
            - name: ARUN_LLM_DEFAULT_PRESET
              value: "{{ .Values.llm.defaultPreset }}"
            - name: ARUN_LLM_PRESETS
              value: {{ toJson .Values.llm.presets | quote }}
            - name: ARUN_HOME
              value: "{{ default "/home/arun/.arun" (get $env "ARUN_HOME") }}"
            - name: ARUN_PUBLIC_URL
              value: "{{ get $env "ARUN_PUBLIC_URL" }}"
            - name: ARUN_ADMIN_USERS
              value: "{{ get $env "ARUN_ADMIN_USERS" }}"
            - name: ARUN_ORCHESTRATE_SUBTASK_TIMEOUT
              value: "{{ default "10m" (get $env "ARUN_ORCHESTRATE_SUBTASK_TIMEOUT") }}"
            - name: ARUN_ORCHESTRATE_PLAN_TIMEOUT
              value: "{{ default "90s" (get $env "ARUN_ORCHESTRATE_PLAN_TIMEOUT") }}"
            - name: QDRANT_URL
              value: "{{ .Values.env.QDRANT_URL }}"
            - name: QDRANT_API_KEY
              value: "{{ .Values.env.QDRANT_API_KEY }}"
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: GITHUB_TOKEN
                  optional: true
            - name: GITHUB_APP_ID
              value: "{{ .Values.env.GITHUB_APP_ID }}"
            - name: GITHUB_APP_INSTALLATION_ID
              value: "{{ .Values.env.GITHUB_APP_INSTALLATION_ID }}"
            - name: GITHUB_APP_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: GITHUB_APP_PRIVATE_KEY
                  optional: true
            - name: GITHUB_APP_PRIVATE_KEY_FILE
              value: "{{ .Values.env.GITHUB_APP_PRIVATE_KEY_FILE }}"
            - name: ARUN_AUTH_REQUIRED
              value: "{{ .Values.auth.required }}"
            - name: GITHUB_OAUTH_CLIENT_ID
              value: "{{ .Values.auth.github.clientId }}"
            - name: GITHUB_OAUTH_CALLBACK_URL
              value: "{{ .Values.auth.github.callbackUrl }}"
            - name: GITHUB_OAUTH_AUTHORIZE_URL
              value: "{{ .Values.auth.github.authorizeUrl }}"
            - name: GITHUB_OAUTH_TOKEN_URL
              value: "{{ .Values.auth.github.tokenUrl }}"
            - name: GITHUB_OAUTH_USER_URL
              value: "{{ .Values.auth.github.userUrl }}"
            - name: GITHUB_OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: GITHUB_OAUTH_CLIENT_SECRET
                  optional: true
            - name: ARUN_SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: ARUN_SESSION_SECRET
                  optional: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: runs
              mountPath: /home/arun/.arun
      volumes:
        - name: runs
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "arun.fullname" . }}
          {{- else }}
          emptyDir: {}
          {{- end }}
