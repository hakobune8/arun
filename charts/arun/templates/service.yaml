apiVersion: v1
kind: Service
metadata:
  name: {{ include "arun.fullname" . }}
  labels:
    {{- include "arun.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arun.selectorLabels" . | nindent 4 }}
